#Projeto prático sobre websocket

O projeto aluraDocs se trata de uma aplicação web moderna onde utiliza o protocolo websocket. 
O objetivo da aplicação é permitir que diversos usuários se comuniquem em tempo real sem precisar dá reflesh.

Para instalar as dependências do projeto use o seguinte comando: npm i
Para rodar no seu navegador use o seguinte comando: npm run dev
